## The file responsible for gitlab continuous integration ##

image: python:3.4

before_script:
  - pip install requests

stages:
  - test

test:
  stage: test
  script:
    - python setup.py runTests
